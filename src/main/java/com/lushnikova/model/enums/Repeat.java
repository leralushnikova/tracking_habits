package com.lushnikova.model.enums;

//Я бы рада исправить папку на enum вместо ENUM, enums, enum_for_model. Но идея на мое исправление выдает ошибку, и не дает работать дальше.
//Заранее извиняюсь, что не знаю как побороть этот баг

/**
 * enum для частоты повторений
 * daily - ежедневно
 * weekly - еженедельно
 */
public enum Repeat {
    DAILY, WEEKLY
}
